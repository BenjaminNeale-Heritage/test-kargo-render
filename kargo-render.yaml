configVersion: v1alpha1
branchConfigs:
- pattern: cluster/([\w-]+)/components/([\w-]+)
  appConfigs:
    component:
      configManagement:
        path: clusters/overlays/${1}/${2}
      outputPath: components/${2}
